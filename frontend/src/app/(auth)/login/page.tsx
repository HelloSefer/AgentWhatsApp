import { AuthRoutePlaceholder } from "@/features/auth/components/auth-route-placeholder";

export default function LoginPage() {
  return (
    <AuthRoutePlaceholder
      description="The login flow will be available here."
      title="Welcome back"
    />
  );
}
