import { madInputToMinor, minorToMadInput } from "./product-money";
import type { SellerProduct, SellerProductCreateInput, SellerProductWriteInput } from "../types/product-contracts";
import type { ProductFormState } from "../types/product-form.types";
export function productToForm(product: SellerProduct): ProductFormState { return { name: product.name, description: product.description ?? "", priceMad: minorToMadInput(product.price.amountMinor), availability: product.availability, options: product.options.map((option) => ({ optionId: option.optionId, label: option.label, required: option.required, values: option.values.map((value) => ({ valueId: value.valueId, label: value.label, isAvailable: value.isAvailable })) })), aliases: [...product.aliases], offers: product.offers.map((offer) => ({ offerId: offer.offerId, label: offer.label, requiredItemCount: String(offer.requiredItemCount), totalMad: minorToMadInput(offer.totalPriceAmountMinor), active: offer.active, allowMixedOptions: offer.allowMixedOptions, priority: String(offer.priority ?? 0), startsAt: offer.startsAt ? offer.startsAt.slice(0, 16) : "", endsAt: offer.endsAt ? offer.endsAt.slice(0, 16) : "" })) }; }
export function emptyProductForm(): ProductFormState { return { name: "", description: "", priceMad: "", availability: "available", options: [], aliases: [], offers: [] }; }
export function formToWrite(form: ProductFormState): SellerProductWriteInput | null { const amountMinor = madInputToMinor(form.priceMad); if (amountMinor === null) return null; return { name: form.name.trim(), description: form.description.trim() || null, price: { amountMinor, currencyCode: "MAD" }, availability: form.availability, options: form.options.map((option, index) => ({ optionId: option.optionId, label: option.label.trim(), required: option.required, position: index, values: option.values.map((value, valueIndex) => ({ valueId: value.valueId, label: value.label.trim(), position: valueIndex, isAvailable: value.isAvailable })) })), aliases: form.aliases.map((alias) => alias.trim()).filter(Boolean), offers: form.offers.map((offer) => ({ offerId: offer.offerId, label: offer.label.trim(), requiredItemCount: Number(offer.requiredItemCount), totalPriceAmountMinor: madInputToMinor(offer.totalMad) ?? -1, active: offer.active, allowMixedOptions: offer.allowMixedOptions, priority: Number(offer.priority), ...(offer.startsAt ? { startsAt: new Date(offer.startsAt).toISOString() } : {}), ...(offer.endsAt ? { endsAt: new Date(offer.endsAt).toISOString() } : {}) })) }; }
export function formToCreate(productId: string, form: ProductFormState): SellerProductCreateInput | null { const write = formToWrite(form); return write ? { productId, ...write } : null; }
